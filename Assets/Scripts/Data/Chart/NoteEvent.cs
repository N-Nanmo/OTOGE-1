using UnityEngine;
namespace RhythmGame.Data.Chart {
    public enum NoteType {
        Tap = 0,
        HoldStart = 1,
        Scratch = 2,
        HoldEnd = 3,
        Flick = 4,
        NoteEnd = 5
        // Scratchån
    }
}

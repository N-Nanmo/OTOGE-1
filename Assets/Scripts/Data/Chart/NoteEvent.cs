using UnityEngine;
namespace RhythmGame.Data.Chart {
    public enum NoteType {
        Tap = 0,
        HoldStart = 1,
        NoteEnd = 2,
        HoldEnd = 3,
        Flick = 4,
        // Scratchån
    }
}
